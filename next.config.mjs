/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.pinimg.com",
      "images.unsplash.com",
      "rwqdczoljaydvxfstuwr.supabase.co",
    ],
  },
};

export default nextConfig;
